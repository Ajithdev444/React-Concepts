import React from 'react'
import Props1 from './Props1'

const Props = ({name, age, email}) => {
  return (

    <div>
     <Props1 name={name} age={age} email={email} />
    </div> 
  )
}

export default Props